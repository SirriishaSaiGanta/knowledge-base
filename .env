# Supabase project connection details. Safe to commit: this is the anon/publishable key, which is
# meant to be public — access control is enforced server-side by the Row Level Security policies on
# the `nodes` and `tags` tables (see the SQL run in the Supabase SQL Editor during setup), not by
# keeping this key secret.
VITE_SUPABASE_URL=https://zyfzeewgrryuxtitdqqz.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_Xq12be93YIPjGlk6Um1y-g_UvPGNyRb
